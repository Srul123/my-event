import {
  REGISTER_SUCCESS,
  REGISTER_FAIL,
  USER_LOADED,
  AUTH_ERROR,
  LOGIN_SUCCESS,
  LOGIN_FAIL,
  LOGOUT,
  CLEAR_ERRORS
} from "./types";

// Load User
export const getGroups = () => async dispatch => {
  try {
  } catch (error) {}
};

// Register User
export const register = group => async dispatch => {
  try {
  } catch (error) {}
};

// Login User
export const deleteGroup = id => async dispatch => {
  try {
  } catch (error) {}
};

// Logout

// Clear Errors
